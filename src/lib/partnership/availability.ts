import type { Availability } from './types';

// Update these flags via the admin workflow when exclusivity slots change.
export const availability: Availability = {
	platinumAvailable: true,
	codejamNamePartnerAvailable: true
};
